{% extends 'layout.html.twig' %}

{% block title %}

{% endblock %}

{% block main %}

    <div class="container">

        <div class="col-md-4 mx-auto">
            <div class="text-center ">
                <h2 class="h2 mb-3 font-weight-normal">{{ controller_name }} {{ sortie.nom }}</h2>
            </div>
        </div>
        {{ form_start(sortieCancel) }}
        <div class="form-group">
            {{ form_row(sortieCancel.nom) }}
            {{ form_row(sortieCancel.dateDebut) }}
            {{ form_row(sortieCancel.dateCloture) }}
            {{ form_row(sortieCancel.lieuSortie) }}
            {{ form_row(sortieCancel.campus) }}
            {{ form_row(sortieCancel.descriptionInfos) }}
        </div>
        <div class="form-group text-right">
            <button class=" btn btn-danger  btn-outline send-button tx-tfm" type="submit">Annuler</button>
            <button class=" btn btn-success btn-outline send-button tx-tfm" type="submit">Valider</button>
        </div>
        {{ form_end(sortieCancel) }}
    </div>
{% endblock %}

